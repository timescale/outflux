language: go

services:
  - docker

go:
  - 1.10.x
  - 1.11.x

before_install:
  # Setup dependency management tool
  - curl -L -s https://github.com/golang/dep/releases/download/v0.5.0/dep-linux-amd64 -o $GOPATH/bin/dep
  - chmod +x $GOPATH/bin/dep
  - dep ensure
  # Start influxdb timescale in docker
<<<<<<< HEAD
  - docker stop `docker ps -a -q`
  - docker rm `docker ps -a -q`
=======
  - docker stop $(docker ps -a -q)
  - docker rm $(docker ps -a -q)
>>>>>>> 3f1bc11d18703c7e0ad0170f87c5cf65b0db03fa
  - docker run -d -p 8086:8086 influxdb
  - docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=postgres timescale/timescaledb

script:
  # Unit tests
  - go test -race -v ./...